#include <stdio.h>
int main(){
float a,b,hip;
float sqrt = 0.5;
printf("introduzca el numero (a): ");
scanf("%f",&a);
printf("introduzca el numero (b): ");
scanf("%f",&b);
hip = ((sqrt)*((a*a) + (b*b)));
printf("%f\n",hip);
return 0;
}

